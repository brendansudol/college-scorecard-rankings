to recreate cleaned dataset, follow these steps:

    virtualenv venv
    source venv/bin/activate
    pip install -r requirements.txt
    cd ipy-notebooks
    ipython notebook

then open up `munge` and run it :)